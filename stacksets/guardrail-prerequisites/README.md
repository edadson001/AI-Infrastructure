# Guardrail Prerequisites

Deploy this StackSet into dev spoke accounts before enabling Service Catalog provisioning. It creates the baseline launch role expected by the `data-pipeline-starter` product registration flow.
