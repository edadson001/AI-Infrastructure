# Firewall Policy Regression

Before promoting firewall changes, confirm the dev firewall stack still deploys cleanly and that the active `spoke-a` path continues to reach approved AWS APIs and Service Catalog provisioning endpoints.
