# Allowed Egress Validation

Provision the `data-pipeline-starter` product into `spoke-a` and verify Glue and Lambda can access the raw and curated buckets and emit logs without opening direct ingress paths.
