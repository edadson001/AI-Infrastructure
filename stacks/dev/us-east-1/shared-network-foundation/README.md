# Shared Network Foundation

This stack is the first deployable layer in the dev region. It exports the shared KMS key, logging bucket, CloudWatch log group, and flow logs IAM role used by downstream stacks.
