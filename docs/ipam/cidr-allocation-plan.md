# CIDR Allocation Plan

Reserve non-overlapping regional CIDR blocks for the shared inspection VPC and for each spoke. Keep attachment, private, firewall, and public subnet ranges distinct per availability zone.
